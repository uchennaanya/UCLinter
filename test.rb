class Car
  def initialize(name, model)
    @name = name
    @model = model
  end

  def valid_function(size)
    size.each do |i|
      puts i
    end
  end
end

class Car
  def initialize(name, model)
    @name = name]
    @model = model
  end

  def invalid_function(size)
    size.each do |i| 
      puts i
    end

  end
